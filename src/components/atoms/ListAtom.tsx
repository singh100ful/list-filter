import * as React from 'react';
import {View, Text} from 'react-native';

interface ListAtomProps {}

const ListAtom: React.FC<ListAtomProps> = ({}) => {
  return (
    <View>
      <Text>ListAtom Screen</Text>
    </View>
  );
};

export default ListAtom;
